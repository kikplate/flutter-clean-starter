# Presentation package folder structure

In new folder structure it will be like presented below:
```
📦 app
├─ features
  └─ [feature_domain_name]
     ├─ views
     ├─ wms
     ├─ models
     └─ themes

```
©generated by [Project Tree Generator](https://woochanleee.github.io/project-tree-generator)


Folder `app` now is a part of main package. It will be placed under `lib` of main package.
All feature component will be placed here. They should be written according `MVVM`.

Pages will be placed separatelly. And folder structure should accord router hierarchy.

For `ui-kit` was created package `oculus`. Folder structure of this package represents below:

```
📦 oculus
├─ lib
│  ├─ assets
│  │  └─ icons
│  └─ src
│     ├─ resources
│     │  └─ icons
│     ├─ themes
│     │  ├─ components
│     │  ├─ schemata
│     │  └─ typography
│     ├─ tokens
│     │  ├─ dimens
│     │  ├─ palettes
│     │  └─ shadows
│     └─ widgets
│        ├─ dimes
│        ├─ icons
│        └─ typography
└─ widgetbook
```

`Oculus` is separated package for ui kit.

Parts in `[ ]` are required. In `( )` are optional
**For some feature:**
```
📦 lib
└─ app
   └─ [feature domain name]
      ├─ [subdomain name]
      │  ├─ views
      │  ├─ wms
      │  ├─ models
      │  └─ ...
      ├─ general
      │  ├─ views
      │  ├─ wms
      │  ├─ models 
      │  └─ ...
      └─ core
         ├─ views
         ├─ wms
         └─ models
```

**For some app general components**
```
📦 lib
└─ app
   ├─ general
   │  ├─ [general widget name]
   │  ├─ [view name]
   │  └─ ...
   ├─ [feature name]
   └─ ...
```

Widgets in general components can be wrappers of default flutter widgets to implement `view` layer in MVVM pattern. For example: `CheckboxTileView`